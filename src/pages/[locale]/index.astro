---
import BaseLayout from "@/layouts/BaseLayout.astro";
import Hero from "@/components/sections/Hero.astro";
import About from "@/components/sections/About.astro";
import Projects from "@/components/sections/Projects.astro";
import Stack from "@/components/sections/Stack.astro";
import Experience from "@/components/sections/Experience.astro";
import Education from "@/components/sections/Education.astro";
import Contact from "@/components/sections/Contact.astro";
import PersonSchema from "@/components/PersonSchema.astro";
import { hasPublishedPosts } from "@/lib/content";
import { locales, useTranslations, type Locale } from "@/i18n/index";

export function getStaticPaths() {
    return locales.map((locale) => ({ params: { locale } }));
}

const locale = Astro.params.locale as Locale;
const t = useTranslations(locale);
const showBlog = await hasPublishedPosts(locale);

// Sólo el nombre en la pestaña: se corta a los pocos caracteres y el oficio ya
// va en la descripción, que es lo que se lee en los resultados de búsqueda.
const title = t("site.title");
---

<BaseLayout
    locale={locale}
    title={title}
    description={t("site.description")}
    showBlog={showBlog}
    onHome
>
    <PersonSchema slot="head" locale={locale} />

    <Hero locale={locale} />
    <About locale={locale} />
    <Projects locale={locale} />
    <Stack locale={locale} />
    <Experience locale={locale} />
    <Education locale={locale} />
    <Contact locale={locale} />
</BaseLayout>
